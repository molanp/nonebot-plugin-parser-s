<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ result.title }}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&family=Segoe+UI:wght@400;600;700&display=swap");

        :root {
            --primary-color: #ff6b6b;
            --secondary-color: #4ecdc4;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --gray-color: #95a5a6;
            --border-radius: 12px;
            --box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Noto Sans SC', sans-serif;
        }
        
        body {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            background: transparent; 
            padding: 20px;
        }
        
        .card {
            width: 750px;
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            overflow: hidden;
            position: relative;
            margin-bottom: 20px;
            border: 1px solid rgba(0,0,0,0.02);
        }
        
        /* 装饰性条纹 */
        .decorative-stripe {
            position: absolute;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--primary-color) 0%, var(--secondary-color) 50%, #9b59b6 100%);
            top: 0;
            left: 0;
            z-index: 5;
        }
        
        /* 中部内容区域 */
        .content-section {
            padding: 30px;
        }
        
        /* 作者信息与二维码区域 */
        .author-qr-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 20px;
            border-bottom: 1px dashed #eee;
        }
        
        .author-info {
            display: flex;
            align-items: center;
            gap: 16px;
        }
        
        .author-avatar {
            width: 64px;
            height: 64px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #fff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        
        .author-details h3 {
            font-size: 19px;
            margin-bottom: 6px;
            color: var(--dark-color);
            font-weight: 700;
        }
        
        .author-meta {
            color: var(--gray-color);
            font-size: 13px;
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }
        
        .info-badge {
            background-color: #f1f8ff;
            padding: 2px 8px;
            border-radius: 6px;
            font-weight: 600;
            font-family: Consolas, Monaco, monospace;
            font-size: 12px;
        }

        .info-badge.content-id { 
            color: #e67e22; 
            background-color: #fdf2e9; 
        }
        
        .time-text {
            display: flex; 
            align-items: center; 
            gap: 4px;
        }
        .time-text::before {
            content: "\f017";
            font-family: "Font Awesome 6 Free";
            font-weight: 400;
        }
        
        .qrcode-placeholder {
            display: flex; 
            flex-direction: column; 
            align-items: flex-end; 
            gap: 4px;
            color: #ddd;
        }
        .qrcode-icon { 
            font-size: 32px; 
            color: #eee; 
        }
        
        /* 统计数据区域 */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 12px;
            margin-bottom: 25px;
        }
        
        .stat-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            background-color: #f8f9fa;
            border-radius: 10px;
            border-left: 4px solid transparent;
        }
        
        .stat-item:nth-child(1) { border-left-color: #9b59b6; }
        .stat-item:nth-child(2) { border-left-color: #f39c12; }
        .stat-item:nth-child(3) { border-left-color: #e74c3c; }
        .stat-item:nth-child(4) { border-left-color: #3498db; }
        .stat-item:nth-child(5) { border-left-color: #2ecc71; }
        
        .stat-icon {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            display: flex; 
            align-items: center; 
            justify-content: center;
            font-size: 16px;
            color: white; 
            flex-shrink: 0;
        }
        
        .stat-icon.play { background-color: #9b59b6; }
        .stat-icon.view { background-color: #f39c12; }
        .stat-icon.like { background-color: #e74c3c; }
        .stat-icon.comment { background-color: #3498db; }
        .stat-icon.share { background-color: #2ecc71; }
        
        .stat-content { 
            display: flex; 
            flex-direction: column; 
        }
        .stat-value { 
            font-size: 17px; 
            font-weight: 700; 
            color: var(--dark-color); 
            line-height: 1.1; 
        }
        .stat-label { 
            font-size: 12px; 
            color: var(--gray-color); 
            margin-top: 2px; 
        }

        /* 内容标题 */
        .content-title {
            font-size: 22px;
            color: var(--dark-color);
            margin-bottom: 16px;
            line-height: 1.4;
            font-weight: 700;
            padding-left: 12px;
            border-left: 4px solid var(--secondary-color);
        }

        /* 正文描述 */
        .description {
            font-size: 15px;
            line-height: 1.7;
            color: #444;
            margin-bottom: 20px;
            white-space: pre-wrap;
        }
        
        /* 图片展示区域 */
        .images-container { 
            margin: 20px 0; 
        }
        .images-header {
            font-size: 14px; 
            color: var(--dark-color); 
            font-weight: 600; 
            margin-bottom: 10px;
            display: flex; 
            align-items: center; 
            gap: 8px;
        }
        .images-grid {
            display: grid; 
            gap: 6px; 
            width: 100%; 
            border-radius: 8px; 
            overflow: hidden;
        }
        .images-grid.single { grid-template-columns: 1fr; }
        .images-grid.single .image-item { max-height: none; /* 取消高度限制 */ }
        .images-grid.double { grid-template-columns: 1fr 1fr; }
        .images-grid.double .image-item { aspect-ratio: 16/9; }
        .images-grid.quad { grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; }
        .images-grid.quad .image-item { aspect-ratio: 1/1; }
        .images-grid.nine { grid-template-columns: repeat(3, 1fr); }
        .images-grid.nine .image-item { aspect-ratio: 1/1; }
        
        .image-item { 
            position: relative; 
            overflow: hidden; 
            background: #eee; 
        }
        .image-item img { 
            width: 100%; 
            height: 100%; 
            object-fit: cover; 
            display: block; 
        }
        .more-count {
            position: absolute; 
            top: 0; 
            left: 0; 
            width: 100%; 
            height: 100%;
            background: rgba(0, 0, 0, 0.5); 
            backdrop-filter: blur(2px);
            color: white; 
            display: flex; 
            align-items: center; 
            justify-content: center;
            font-size: 28px; 
            font-weight: bold;
        }
        
        /* 底部信息栏 */
        .footer-section {
            background-color: var(--dark-color);
            color: rgba(255,255,255,0.8);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 12px;
        }
        .footer-info { 
            display: flex; 
            gap: 15px; 
            align-items: center; 
        }
        .footer-brand { 
            font-weight: 600; 
            letter-spacing: 0.5px; 
            color: white; 
            display: flex; 
            gap: 8px; 
            align-items: center;
        }
        
        /* 评论区域 */
        .comments-section {
            padding: 0 30px 30px;
        }
        
        .comments-header {
            font-size: 18px;
            font-weight: 700;
            color: var(--dark-color);
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .comments-header i {
            color: var(--secondary-color);
        }
        
        .comment-item {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px dashed #eee;
        }
        
        .comment-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .comment-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            object-fit: cover;
            flex-shrink: 0;
            margin-top: 2px;
        }
        
        .comment-content {
            flex: 1;
        }
        
        .comment-author-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 8px;
        }
        
        .comment-author-section {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .comment-author {
            font-weight: 600;
            font-size: 14px;
            color: var(--dark-color);
        }
        
        .comment-badge {
            width: 16px;
            height: 16px;
            object-fit: contain;
        }
        
        .comment-time {
            font-size: 12px;
            color: var(--gray-color);
        }
        
        .comment-honor {
            font-size: 12px;
            color: var(--secondary-color);
            margin-left: 8px;
        }
        
        .comment-meta {
            font-size: 12px;
            color: #999;
            margin-top: 4px;
        }
        
        .comment-text {
            font-size: 15px;
            line-height: 1.6;
            color: #444;
            margin-bottom: 10px;
            word-wrap: break-word;
        }
        
        .comment-stats {
            display: flex;
            gap: 20px;
            font-size: 12px;
            color: var(--gray-color);
        }
        
        .comment-stat {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        /* 回复区域 */
        .comment-replies {
            margin-left: 63px;
            margin-top: 15px;
        }
        
        .reply-item {
            display: flex;
            gap: 12px;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px dashed #f0f0f0;
        }
        
        .reply-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }
        
        .reply-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            flex-shrink: 0;
            margin-top: 2px;
        }
        
        .reply-content {
            flex: 1;
        }
        
        .reply-author-row {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 6px;
        }
        
        .reply-author {
            font-weight: 600;
            font-size: 13px;
            color: var(--dark-color);
        }
        
        .reply-time {
            font-size: 11px;
            color: var(--gray-color);
        }
        
        .reply-text {
            font-size: 14px;
            line-height: 1.6;
            color: #444;
            margin-bottom: 8px;
            word-wrap: break-word;
        }
        
        /* 表情样式 */
        .comment-text img,
        .reply-text img {
            display: inline-block;
            vertical-align: middle;
            width: 20px;
            height: 20px;
            margin: 0 2px;
        }
    </style>
</head>
<body>

    {# === 宏定义：解析评论内容 === #}
    {% macro parse_comment_content(content_json) %}
    {% set text = "" %}
    {% if content_json %}
        {% for item in content_json %}
            {% if item.type == "paragraph" %}
                {% for child in item.children %}
                    {% if child.text %}
                        {% set text = text + child.text %}
                    {% endif %}
                {% endfor %}
            {% endif %}
        {% endfor %}
    {% elif content_json is string %}
        {% set text = content_json %}
    {% endif %}
    {{ text }}
    {% endmacro %}
    
    {# === 宏定义：渲染卡片 === #}
    {% macro render_card(result) %}
    
    {# 变量定义 #}
    {% set extra = result.extra if result.extra else {} %}
    {% set stats = extra.stats if extra.stats else {} %}
    {% set comments = result.comments if result.comments else [] %}
    {% if not comments and extra.comments %}
        {% set comments = extra.comments %}
    {% endif %}

    <div class="card">
        <div class="decorative-stripe"></div>

        {# --- 封面区域 --- #}
        {% set cover_url = result.extra.video_cover %}
        {% set has_video = cover_url and cover_url != '' %}
        {% if not cover_url and result.extra.images and result.extra.images|length > 0 %}
            {% set cover_url = result.extra.images[0] %}
        {% endif %}

        {% if cover_url %}
        <div class="cover-section" style="position: relative; width: 100%; height: 300px; overflow: hidden;">
            <img src="{{ cover_url }}" style="width: 100%; height: 100%; object-fit: cover;" alt="Cover">
            <div class="cover-overlay" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(to bottom, transparent, rgba(0,0,0,0.3));"></div>
            
            <!-- 类型标签或SEO关键词 -->
            <div class="tag type-tag" style="position: absolute; top: 15px; left: 15px; background: rgba(0,0,0,0.6); color: white; padding: 5px 10px; border-radius: 4px; font-size: 12px; display: flex; align-items: center; gap: 5px;">
                {% set has_footer_images = extra.footer_images is defined and extra.footer_images|length > 0 %}
                {% if has_footer_images and extra.seo_keywords and extra.seo_keywords != '' %}
                <span>{{ extra.seo_keywords }}</span>
                {% elif has_video %}
                <i class="fas fa-video"></i>
                <span>视频</span>
                {% else %}
                <i class="fas fa-images"></i>
                <span>图文</span>
                {% endif %}
            </div>
            
            <!-- 平台标签 -->
            <div class="tag platform-tag" style="position: absolute; top: 15px; right: 15px; background: rgba(0,0,0,0.6); color: white; padding: 5px 10px; border-radius: 4px; font-size: 12px; display: flex; align-items: center; gap: 5px;">
                <img src="https://www.taptap.cn/favicon.ico" style="width: 16px; height: 16px; object-fit: contain;">
                <span>TAPTAP</span>
            </div>
            
            <!-- 播放按钮 -->
            {% if has_video %}
            <div class="play-btn-overlay" style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: rgba(0,0,0,0.6); border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; cursor: pointer;">
                <i class="fas fa-play" style="color: white; font-size: 24px; margin-left: 5px;"></i>
            </div>
            {% endif %}
        </div>
        {% endif %}

        <div class="content-section">
            {# --- 作者信息行 --- #}
            {% if result.author or result.extra.author %}
            {% set author_data = result.extra.author if result.extra.author else {} %}
            <div class="author-qr-row">
                <div class="author-info">
                    {% if result.author.avatar or author_data.avatar %}
                    <img src="{{ result.author.avatar or author_data.avatar }}" class="author-avatar">
                    {% else %}
                    <div class="author-avatar" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);"></div>
                    {% endif %}
                    <div class="author-details">
                        <h3>{{ result.author.name or author_data.name }}</h3>
                        <div class="author-meta">
                            {% if result.formatted_datetime %}
                            <span class="time-text">{{ result.formatted_datetime }}</span>
                            {% elif result.extra.formatted_publish_time %}
                            <span class="time-text">{{ result.extra.formatted_publish_time }}</span>
                            {% elif result.extra.formatted_created_time %}
                            <span class="time-text">{{ result.extra.formatted_created_time }}</span>
                            {% endif %}
                            {% if author_data.app_title %}
                            <span class="info-badge">
                                {% if author_data.app_icon %}<img src="{{ author_data.app_icon }}" style="width: 14px; height: 14px; vertical-align: middle; margin-right: 4px;">{% endif %}
                                {{ author_data.app_title }}
                            </span>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="qrcode-placeholder">
                {% if result.qr_code_path %}
                    <img src="{{ result.qr_code_path }}" alt="QR Code" style="width: 80px; height: 80px; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);" />
                {% else %}
                    <i class="fas fa-qrcode qrcode-icon"></i>
                {% endif %}
                </div>
            </div>
            {% endif %}

            {# --- 游戏信息 --- #}
            {% if result.extra.app %}
            {% set app = result.extra.app %}
            <div style="margin: 20px 0; padding: 15px; background-color: #f8f9fa; border-radius: 8px; border-left: 4px solid #4ecdc4;">
                <div style="display: flex; align-items: flex-start; gap: 15px;">
                    {% if app.icon %}
                    <img src="{{ app.icon }}" style="width: 60px; height: 60px; border-radius: 8px; object-fit: cover;">
                    {% endif %}
                    <div style="flex: 1;">
                        <h4 style="font-size: 16px; color: #2c3e50; margin-bottom: 8px;">{{ app.title }}</h4>
                        {% if app.rating %}
                        <div style="display: flex; gap: 12px; margin-bottom: 8px;">
                            <span style="font-size: 14px; color: #e67e22; font-weight: 600;">当前评分: {{ app.rating }}</span>
                            {% if app.latest_score %}
                            <span style="font-size: 14px; color: #2ecc71; font-weight: 600;">历史最高分: {{ app.latest_score }}</span>
                            {% endif %}
                        </div>
                        {% endif %}
                        {% if app.tags %}
                        <div style="display: flex; gap: 8px; flex-wrap: wrap;">
                            {% for tag in app.tags[:5] %}
                            <span style="font-size: 12px; color: #3498db; background-color: #e3f2fd; padding: 2px 8px; border-radius: 12px;">{{ tag.value }}</span>
                            {% endfor %}
                            {% if app.tags|length > 5 %}
                            <span style="font-size: 12px; color: #95a5a6; padding: 2px 8px;">+{{ app.tags|length - 5 }}</span>
                            {% endif %}
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endif %}

            {# --- 统计数据 --- #}
            {% if stats %}
            {% set stat_items = [] %}

            {# 浏览数 #}
            {% if stats.views %}
                {% set _ = stat_items.append({'key': 'views', 'value': stats.views, 'label': '浏览', 'icon': 'fa-eye', 'color': '#f39c12', 'cls': 'view'}) %}
            {% endif %}

            {# 播放数 #}
            {% if stats.plays %}
                {% set _ = stat_items.append({'key': 'plays', 'value': stats.plays, 'label': '播放', 'icon': 'fa-play', 'color': '#9b59b6', 'cls': 'play'}) %}
            {% endif %}

            {# 点赞数 #}
            {% if stats.likes %}
                {% set _ = stat_items.append({'key': 'likes', 'value': stats.likes, 'label': '点赞', 'icon': 'fa-thumbs-up', 'color': '#e74c3c', 'cls': 'like'}) %}
            {% endif %}

            {# 评论数 #}
            {% if stats.comments %}
                {% set _ = stat_items.append({'key': 'comments', 'value': stats.comments, 'label': '评论', 'icon': 'fa-comment', 'color': '#3498db', 'cls': 'comment'}) %}
            {% endif %}

            {# 分享数 #}
            {% if stats.shares %}
                {% set _ = stat_items.append({'key': 'shares', 'value': stats.shares, 'label': '分享', 'icon': 'fa-share', 'color': '#2ecc71', 'cls': 'share'}) %}
            {% endif %}

            {# 只在有统计项时显示整个统计容器 #}
            {% if stat_items %}
            <div class="stats-container">
                {% for item in stat_items %}
                <div class="stat-item" style="border-left-color: {{ item.color }};">
                    <div class="stat-icon {{ item.cls }}">
                        <i class="fas {{ item.icon }}"></i>
                    </div>
                    <div class="stat-content">
                        <div class="stat-value">{{ item.value }}</div>
                        <div class="stat-label">{{ item.label }}</div>
                    </div>
                </div>
                {% endfor %}
            </div>
            {% endif %}
            {% endif %}

            {# --- 主要标题 --- #}
            {% if result.title %}
            <div class="content-title">{{ result.title }}</div>
            {% endif %}

            {# --- 正文描述 --- #}
            {% if result.text %}
            <div class="description">{{ result.text }}</div>
            {% endif %}

            {# --- 内容媒体展示 --- #}
            {% set has_video = result.extra.video_cover and result.extra.video_cover != '' %}
            {% set imgs = result.extra.images %}
            {% set count = imgs|length if imgs else 0 %}
            
            {# 只有图文内容才显示图片列表，视频的话下面不放图片 #}
            {% if not has_video and count > 0 %}
            <div class="images-container">
                <div class="images-header">
                    <i class="fas fa-images" style="color:#2ecc71"></i> 内容图片
                </div>
                
                {% set grid_class = 'single' %}
                {% if count == 2 %}{% set grid_class = 'double' %}
                {% elif count == 4 %}{% set grid_class = 'quad' %}
                {% elif count >= 3 %}{% set grid_class = 'nine' %}
                {% endif %}

                <div class="images-grid {{ grid_class }}">
                    {% for image in imgs[:9] %}
                    <div class="image-item">
                        <img src="{{ image }}">
                        {% if loop.last and count > 9 %}
                        <div class="more-count">+{{ count - 9 }}</div>
                        {% endif %}
                    </div>
                    {% endfor %}
                </div>
            </div>
            {% endif %}
        </div>
        
        {# --- 评论区域 --- #}
        {% if comments and comments|length > 0 %}
        <div class="comments-section">
            <div class="comments-header">
                <i class="fas fa-comments"></i> 评论 ({{ comments|length }})
            </div>
            
            {% for comment in comments %}
            <div class="comment-item">
                <img src="{{ comment.author.avatar }}" alt="用户头像" class="comment-avatar">
                <div class="comment-content">
                    <div class="comment-author-row">
                        <div class="comment-author-section">
                            <span class="comment-author">{{ comment.author.name }}</span>
                            
                            {# 徽章展示 #}
                            {% if comment.author.badges and comment.author.badges|length > 0 %}
                                {% for badge in comment.author.badges %}
                                    {% if badge.icon and badge.icon.small %}
                                        <img src="{{ badge.icon.small }}" alt="徽章" class="comment-badge" title="{{ badge.title }}">
                                    {% endif %}
                                {% endfor %}
                            {% endif %}
                            
                            {# 荣誉/称号展示 #}
                            {% if comment.author.honor_title %}
                                <span class="comment-honor">{{ comment.author.honor_title }}</span>
                            {% endif %}
                        </div>
                        <span class="comment-time">{{ comment.formatted_time }}</span>
                    </div>
                    
                    {# 个性签名/其他信息展示 #}
                    {% if comment.author.description %}
                        <div class="comment-meta">{{ comment.author.description }}</div>
                    {% endif %}
                    
                    <div class="comment-text">
                        {{ comment.content }}
                    </div>
                    <div class="comment-stats">
                        {% if comment.ups %}
                        <span class="comment-stat">
                            <i class="fas fa-thumbs-up"></i>
                            <span>{{ comment.ups }}</span>
                        </span>
                        {% endif %}
                        {% if comment.comments %}
                        <span class="comment-stat">
                            <i class="fas fa-reply"></i>
                            <span>{{ comment.comments }}</span>
                        </span>
                        {% endif %}
                    </div>
                    
                    {# 回复列表 #}
                    {% if comment.child_posts and comment.child_posts|length > 0 %}
                    <div class="comment-replies">
                        {% for reply in comment.child_posts %}
                        <div class="reply-item">
                            <img src="{{ reply.author.avatar }}" alt="回复者头像" class="reply-avatar">
                            <div class="reply-content">
                                <div class="comment-author-row">
                                    <div class="comment-author-section">
                                        <span class="comment-author">{{ reply.author.name }}</span>
                                        
                                        {# 回复者徽章展示 #}
                                        {% if reply.author.badges and reply.author.badges|length > 0 %}
                                            {% for badge in reply.author.badges %}
                                                {% if badge.icon and badge.icon.small %}
                                                    <img src="{{ badge.icon.small }}" alt="徽章" class="comment-badge" title="{{ badge.title }}">
                                                {% endif %}
                                            {% endfor %}
                                        {% endif %}
                                        
                                        {# 回复者荣誉/称号展示 #}
                                        {% if reply.author.honor_title %}
                                            <span class="comment-honor">{{ reply.author.honor_title }}</span>
                                        {% endif %}
                                    </div>
                                    <span class="comment-time">{{ reply.formatted_time }}</span>
                                </div>
                                
                                {# 回复者个性签名/其他信息展示 #}
                                {% if reply.author.description %}
                                    <div class="comment-meta">{{ reply.author.description }}</div>
                                {% endif %}
                                
                                <div class="comment-text">
                                    {{ reply.content }}
                                </div>
                                <div class="comment-stats">
                                    {% if reply.ups %}
                                    <span class="comment-stat">
                                        <i class="fas fa-thumbs-up"></i>
                                        <span>{{ reply.ups }}</span>
                                    </span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}

        {# --- 底部 --- #}
        <div class="footer-section">
            <div class="footer-brand">
                <i class="fas fa-layer-group"></i>
                <span>{{ bot_name }} Parser || Designed By LoCCai</span>
            </div>
            <div class="footer-info">
                <span>{{ result.url }}</span>
                {% if rendering_time %}
                <span style="margin-left: 15px; color: rgba(255,255,255,0.6); font-size: 11px;">
                    <i class="fas fa-clock" style="margin-right: 4px;"></i>
                    <span>Rendering at {{ rendering_time }}</span>
                </span>
                {% endif %}
            </div>
        </div>
    </div>
    {% endmacro %}

    {{ render_card(result) }}

</body>
</html>